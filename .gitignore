# Credentials — never commit
secrets/

# Python
__pycache__/
*.py[cod]
.venv/
*.egg-info/
dist/
build/

# Tooling caches
.pytest_cache/
.ruff_cache/
.mypy_cache/

# Local CLI state (cookie jar, quota) if it ever lands in-tree
.cache/

# Claude Code / local-assistant artifacts
CLAUDE.md
**/CLAUDE.md
CLAUDE.local.md
.claude/
